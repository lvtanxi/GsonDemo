<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="com.lv.gsondemo.MainActivity">

        <Button
            style="@style/btn"
            android:onClick="testNormal"
            android:text="Normal"/>

        <Button
            style="@style/btn"
            android:onClick="testSerializedName"
            android:text="SerializedName"/>

        <Button
            style="@style/btn"
            android:onClick="testJsonReader"
            android:text="JsonReader"/>

        <Button
            style="@style/btn"
            android:onClick="testJsonWriter"
            android:text="JsonWriter"/>

        <Button
            style="@style/btn"
            android:onClick="testGsonBuilder"
            android:text="GsonBuilder"/>

        <Button
            style="@style/btn"
            android:onClick="testExpose"
            android:text="Expose"/>

        <Button
            style="@style/btn"
            android:onClick="testTypeAdapter"
            android:text="TypeAdapter"/>

        <Button
            style="@style/btn"
            android:onClick="testJsonDeserializer"
            android:text="JsonDeserializer"/>

        <Button
            style="@style/btn"
            android:onClick="testJsonSerializer"
            android:text="JsonSerializer"/>
    </LinearLayout>
</ScrollView>
